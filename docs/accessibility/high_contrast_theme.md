# High Contrast Theme

The GUI must support a high-contrast theme. Color is never the only signal:
language labels, source markers, and text values must remain visible to screen
readers and keyboard users.
