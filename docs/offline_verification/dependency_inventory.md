# Dependency Inventory

Core Python package dependencies are intentionally minimal.

Optional dependencies:

- `PySide6` for the desktop GUI.
- `openpyxl` for XLSX import/export.
- `pytest` for tests.

Large optional models such as AfroLID or ASR resources must not be committed to
the core source tree.
